---
title: processo-seletivo v1
sidebar_label: processo-seletivo v1
description: Vendor media type application/vnd.uniplus.processo-seletivo.v1+json — representação do agregado ProcessoSeletivo (módulo Seleção).
---

import MediaTypeCard from '@site/src/components/MediaTypeCard';
import RedocSpec from '@site/src/components/RedocSpec';

<MediaTypeCard
  resource="processo-seletivo"
  version="v1"
  vendor_mime="application/vnd.uniplus.processo-seletivo.v1+json"
  status="current"
  schema_href="/openapi/selecao.json#/components/schemas/ProcessoSeletivoDto"
/>

## O que é

Representação do agregado `ProcessoSeletivo` — o certame configurável do módulo
Seleção (não confundir com o edital, o ato publicado; ver
[Edital versus processo seletivo](../../produto/dominio/index.mdx#edital-versus-processo-seletivo)).
Este media type cobre **só as duas operações de leitura** do recurso —
`GET /api/selecao/processos-seletivos/{id}` e `GET /api/selecao/processos-seletivos`
(coleção, paginada) —, cada uma com o schema abaixo:

| Endpoint | Schema |
|---|---|
| `GET /api/selecao/processos-seletivos/{id}` (detalhe) | [`ProcessoSeletivoDto`](pathname:///openapi/selecao.json#/components/schemas/ProcessoSeletivoDto) — o schema declarado acima. |
| `GET /api/selecao/processos-seletivos` (coleção, paginada) | [`ProcessoSeletivoResumoDto[]`](pathname:///openapi/selecao.json#/components/schemas/ProcessoSeletivoResumoDto) — array de resumo, não o mesmo shape do detalhe. |

Validar uma resposta de coleção contra o schema do detalhe (ou vice-versa) falha.

As demais operações do agregado **não** usam este vendor MIME. Duas famílias
distintas:

- **Mutações** (criação, todas as atualizações por seção — etapas, cotas,
  bônus etc. —, publicação, retificação) negociam em JSON comum
  (`application/json`) e, quando têm corpo de resposta, não o versionam por
  vendor MIME — a maioria responde `204 No Content`.
- **Outras leituras** (`GET .../retificacao-em-curso`, `GET .../conformidade`,
  `GET .../conformidade-legal`, `GET .../snapshot-vigente`) têm, cada uma, o
  próprio media type versionado (ex.:
  `application/vnd.uniplus.retificacao-em-curso.v1+json`) — não este.

Elas aparecem na referência abaixo só para dar contexto do agregado completo;
não são descritas por esta entrada.

## Referência completa do agregado

<RedocSpec spec="selecao.json" tag="ProcessoSeletivo" />
