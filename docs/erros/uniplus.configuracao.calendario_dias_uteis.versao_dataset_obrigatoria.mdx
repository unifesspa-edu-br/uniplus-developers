---
title: A versão do dataset do calendário não foi informada
sidebar_label: Versão do dataset obrigatória
description: Ao criar um dataset de calendário de dias úteis, a versão é obrigatória — é o identificador que a versão publicada de um processo seletivo congela.
code: uniplus.configuracao.calendario_dias_uteis.versao_dataset_obrigatoria
http_status: 422
situacao: publicado
modulo: Configuração
emitido_em:
  - Criação de um dataset de calendário de dias úteis
requisitos: []
rastreio:
  - label: 'uniplus-api#1140'
    href: https://github.com/unifesspa-edu-br/uniplus-api/issues/1140
---

import ErrorCatalogEntry from '@site/src/components/ErrorCatalogEntry';

<ErrorCatalogEntry {...frontMatter} />

## O que aconteceu

A operação foi recusada porque a **versão do dataset** não foi preenchida ao
criar um calendário de dias úteis.

O calendário de dias úteis é reference data versionada por dataset inteiro,
não por linha: uma correção nos feriados publica um dataset novo, com a lista
completa de dias não úteis — nunca edita a lista de um dataset existente. Não
existe operação de atualização por design. A versão é o identificador desse
dataset, e é ela (junto com o próprio identificador do registro) que a versão
publicada de um processo seletivo congela quando a contagem de prazo distingue
dia útil de não útil — sem uma versão declarada, não há o que congelar.

## Como resolver

Informe a versão do dataset ao criar o calendário — um rótulo que identifique
essa remessa de feriados (por exemplo, o ano ou o ciclo a que se refere).
