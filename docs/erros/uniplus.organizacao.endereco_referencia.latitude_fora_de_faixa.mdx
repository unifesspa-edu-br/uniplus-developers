---
title: A latitude do endereço está fora da faixa válida
sidebar_label: Latitude do endereço fora da faixa
description: A latitude do endereço, quando informada, precisa estar entre -90 e 90.
code: uniplus.organizacao.endereco_referencia.latitude_fora_de_faixa
http_status: 422
situacao: publicado
modulo: Transversal
emitido_em:
  - Criação ou atualização da instituição, quando o endereço da sede é informado com coordenada
  - Criação ou atualização de campus, quando o endereço é informado com coordenada (Configuração)
  - Criação ou atualização de local de oferta, quando o endereço é informado com coordenada (Configuração)
requisitos: []
rastreio:
  - label: 'uniplus-api#726'
    href: https://github.com/unifesspa-edu-br/uniplus-api/issues/726
---

import ErrorCatalogEntry from '@site/src/components/ErrorCatalogEntry';

<ErrorCatalogEntry {...frontMatter} />

## O que aconteceu

A **latitude** informada está fora da faixa válida: entre -90 e 90 graus
decimais.

Latitude e longitude são opcionais dentro do endereço — o CEP já
resolve a localização a um nível suficiente para a maioria dos usos —, mas
quando informadas precisam ser coordenadas geograficamente válidas.

Esta validação é compartilhada pelo mesmo value object
(`ReferenciaEnderecoGeo`, no Kernel) que valida o endereço de três
cadastros distintos, em dois módulos: a sede da Instituição (Organização
Institucional), o Campus e o Local de Oferta (Configuração). O código
desta página é o que a API hoje emite para os três — a resolução do
`type` segue a chave interna do validador compartilhado, comum aos três
cadastros, e não distingue de qual módulo a chamada partiu.

## Como resolver

Informe uma latitude entre -90 e 90, ou omita a coordenada se ela não
estiver disponível.
