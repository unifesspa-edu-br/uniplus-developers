---
title: Destino de remanejamento só se aplica à regra DESTINO_UNICO
sidebar_label: Destino de remanejamento indevido
description: Só uma modalidade com remanejamento DESTINO_UNICO declara um destino — nas demais regras, o campo não se aplica.
code: uniplus.selecao.modalidade_selecionada.remanejamento_destino_indevido
http_status: 422
situacao: publicado
modulo: Seleção
emitido_em:
  - Definição de distribuição de vagas do processo seletivo
  - Descarte de retificação (restauração da configuração congelada)
requisitos: []
rastreio:
  - label: 'uniplus-api#778'
    href: https://github.com/unifesspa-edu-br/uniplus-api/pull/778
  - label: 'uniplus-api#865'
    href: https://github.com/unifesspa-edu-br/uniplus-api/pull/865
---

import ErrorCatalogEntry from '@site/src/components/ErrorCatalogEntry';

<ErrorCatalogEntry {...frontMatter} />

## O que aconteceu

Uma modalidade selecionada declarou `remanejamentoDestino` sem ter
`regraRemanejamento: DESTINO_UNICO`. Este é o par simétrico de
[`remanejamento_destino_obrigatorio`](./uniplus.selecao.modalidade_selecionada.remanejamento_destino_obrigatorio.mdx):
a mesma coerência, olhada do lado oposto — lá, o campo faltando quando devia
estar presente; aqui, presente quando devia faltar. Nenhuma outra regra de
remanejamento (`CRUZADO`, `SEGUE_CASCATA`, ou modalidade sem regra de
remanejamento declarada) usa este campo.

Não acumula com nenhuma outra violação — é a primeira que falhar que
interrompe a criação da modalidade.

**`regraRemanejamento` e `remanejamentoDestino` não vêm do payload da
definição de distribuição de vagas** — são copiados do cadastro de
modalidades de Configuração, no momento em que a modalidade é selecionada
(`IModalidadeReader`). O próprio cadastro já impede esta incoerência na
escrita (as invariantes cruzadas de `Modalidade`, tanto na criação quanto na
atualização); pela definição de
distribuição de vagas, este código só aparece se a modalidade estiver
gravada no cadastro de um jeito que contorna essa validação — a checagem
aqui é defesa em profundidade, não a primeira linha.

Este código também aparece no **descarte de retificação**: reconstruir o
grafo a partir do envelope congelado recria cada modalidade selecionada a
partir dos mesmos campos — a mesma checagem roda de novo, sozinha.

## Como resolver

O campo em excesso não está no payload que você enviou — está na modalidade
cadastrada em Configuração. Corrija lá a regra de remanejamento ou remova o
destino indevido, ou investigue como a modalidade foi gravada sem essa
coerência.

Se o erro apareceu ao descartar uma retificação, o problema também não está
no payload enviado agora — está na configuração congelada para a qual o
descarte está tentando voltar, refletindo uma modalidade já incoerente no
momento em que foi selecionada.
