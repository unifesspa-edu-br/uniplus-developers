---
title: Modalidade com remanejamento CRUZADO exige par e fallback
sidebar_label: Remanejamento cruzado incompleto
description: O remanejamento CRUZADO troca vagas ociosas entre um par de modalidades e precisa de um fallback para quando o par também não tiver candidato.
code: uniplus.selecao.modalidade_selecionada.remanejamento_cruzado_incompleto
http_status: 422
situacao: publicado
modulo: Seleção
emitido_em:
  - Definição de distribuição de vagas do processo seletivo
  - Descarte de retificação (restauração da configuração congelada)
requisitos: []
rastreio:
  - label: 'uniplus-api#778'
    href: https://github.com/unifesspa-edu-br/uniplus-api/pull/778
  - label: 'uniplus-api#865'
    href: https://github.com/unifesspa-edu-br/uniplus-api/pull/865
---

import ErrorCatalogEntry from '@site/src/components/ErrorCatalogEntry';

<ErrorCatalogEntry {...frontMatter} />

## O que aconteceu

Uma modalidade selecionada declarou `regraRemanejamento: CRUZADO` sem
declarar `remanejamentoPar`, sem declarar `remanejamentoFallback`, ou sem
declarar nenhum dos dois. Diferente de `DESTINO_UNICO` (ver
[`remanejamento_destino_obrigatorio`](./uniplus.selecao.modalidade_selecionada.remanejamento_destino_obrigatorio.mdx)),
que precisa de um único destino, `CRUZADO` é o remanejamento recíproco entre
duas modalidades (ex.: IND↔QUIL, no tipo de processo indígena/quilombola) —
precisa das duas peças: o **par** (a outra modalidade do cruzamento) e o
**fallback** (para onde a vaga vai se nem a modalidade nem o par tiverem
candidato aprovado).

Não acumula com nenhuma outra violação — é a primeira que falhar que
interrompe a criação da modalidade.

**`regraRemanejamento`, `remanejamentoPar` e `remanejamentoFallback` não vêm
do payload da definição de distribuição de vagas** — são copiados do
cadastro de modalidades de Configuração, no momento em que a modalidade é
selecionada (`IModalidadeReader`). O próprio cadastro já impede esta
incoerência na escrita (as invariantes cruzadas de `Modalidade`, tanto na criação quanto na atualização); pela
definição de distribuição de vagas, este código só aparece se a modalidade
estiver gravada no cadastro de um jeito que contorna essa validação — a
checagem aqui é defesa em profundidade, não a primeira linha.

Este código também aparece no **descarte de retificação**: reconstruir o
grafo a partir do envelope congelado recria cada modalidade selecionada a
partir dos mesmos campos — a mesma checagem roda de novo, sozinha.

## Como resolver

Os campos que faltam não estão no payload que você enviou — estão na
modalidade cadastrada em Configuração. Corrija lá o vínculo entre
`regraRemanejamento: CRUZADO` e o par/fallback, ou investigue como a
modalidade foi gravada sem essa coerência.

Se o erro apareceu ao descartar uma retificação, o problema também não está
no payload enviado agora — está na configuração congelada para a qual o
descarte está tentando voltar, refletindo uma modalidade já incoerente no
momento em que foi selecionada.
